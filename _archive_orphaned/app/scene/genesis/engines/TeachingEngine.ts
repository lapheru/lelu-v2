import type { GenesisState } from "../state/GenesisState";

export default class TeachingEngine {
  update(_state: GenesisState, _delta: number): void {
    // Compatibility shim for legacy GenesisManager imports.
  }
}
