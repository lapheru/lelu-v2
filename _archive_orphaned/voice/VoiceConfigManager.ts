/**
 * ==========================================================
 * LÉLU
 * VOICE CONFIG MANAGER
 * ==========================================================
 */

import VoiceConfig from "./VoiceConfig";

export default class VoiceConfigManager {

  readonly config =
    new VoiceConfig();

}